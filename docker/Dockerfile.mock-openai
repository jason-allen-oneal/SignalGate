FROM python:3.11-slim

WORKDIR /app

RUN python -m pip install --no-cache-dir -U pip \
  && python -m pip install --no-cache-dir fastapi uvicorn

COPY docker/mock_openai.py /app/mock_openai.py

EXPOSE 9999

CMD ["uvicorn", "mock_openai:app", "--host", "0.0.0.0", "--port", "9999"]
